import React from 'react';
import { Layout, Space } from 'antd';

const Footer = () => {
	const { Footer } = Layout;
	return (
		<div>
			<Footer>Footer</Footer>
		</div>
	);
};

export default Footer;
